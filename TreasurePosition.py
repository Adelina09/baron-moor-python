#!/usr/bin/env python3

import random, GameBoard, CalculatePosition

class TreasurePosition(GameBoard, CalculatePosition):

    x = 0
    y = 0

    def __init__(self):
        self.x = x
        self.y = y

    def calculateXPosition():
        x = random.randint(rows)
        return x

    def calculateYPosition():
        y = random.randint(columns)
        return y


